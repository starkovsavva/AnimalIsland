package org.example.animals.AnimalList.predator;

import org.example.animals.Predator;

public class Bear extends Predator {

    public Bear(int id, double weight, int maxCount, int speed, int food) {
        super(id, weight, maxCount, speed, food);
    }
}